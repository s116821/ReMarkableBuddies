## 1. Configuration and interface
- [x] 1.1 Restore requested flags, exact inventory/conflicts, environment precedence and early value-free credential validation.
- [x] 1.2 Enable app debug by default with validated level override and RUST_LOG fallback; keep image dumps opt-in.
- [x] 1.3 Remove unsupported production branches; add capture-only and single immediate iteration examples using production components.
## 2. Guidance and regressions
- [x] 2.1 Update public invocations, env and native guidance; inspect service/scripts/fixtures for removed flags.
- [x] 2.2 Run parser/config/logging and spawned offline simulator regressions, positive and negative maintained scenarios; preserve failures.
- [x] 2.3 Run fmt, strict clippy, full host tests and both architecture builds.
## 3. Native and delivery
- [x] 3.1 Verify exact native CLI/capture and service/default logging; bounded example reaches real proposal/independent reading and propagates guarded refusal without answer output; verify safe restoration. Record successful Q&A as unpassed and carry availability investigation to REM9.
- [x] 3.2 Publish grouped actual evidence, re-read chronology and obtain independent implementation/scope review; retain final exact-head review/green CI as external PR merge gates with capped Bugbot disclosed.
- [x] 3.3 Sync canonical requirements in place, check duplicates/markers and archive in the same PR. Track normal merge/release verification before issue Done in the PR and progress record.

Implementation and scoped validation are complete. Final PR checks/review and subsequent merge/release verification remain delivery gates; this checklist does not claim those have already occurred. Successful native Q&A remains unpassed and tracked in REM9.
