## Context

Read REM-32 and its complete comment list, REM-8 description/all five comments, REM-4 description/all three comments and REM-15 description/all three comments. Relevant chronology (UTC): REM-8 Dec 8 2025 07:15 establishes initial/request-sent/response-received nesting; Dec 9 08:13 (updated 11:59) substitutes triangles, 333 ms per segment, retracing and tangent auxiliary circles. Dec 9 09:21 (updated 09:39) adds six failure segments; 11:00 requires cleanup before any viewport change and 11:04 excludes history activity. REM-4 Dec 9 11:58 retains center spokes for context-enhanced requests. Those spokes belong to REM-4. The REM-15 Sep 2026 native append-history availability report remains unresolved and is a validation constraint, not permission to relax ownership.

The Sep 22 audit initially missed REM-13 comment a4675fd3 (Dec 11 2025 00:58:41): model/network diagnostics must never be typed. Main 9c52933 orchestrator.rs:455-457 violates this in the outer loop. This correction includes repeated loop-mode provider, capture and marker-failure regressions; the earlier audit conclusion has been corrected.

The current backend draws one circle and erases that fixed path. Model waits sleep 750 ms after drawing, obscuring actual cadence. Loop errors can type arbitrary error text into the document. The replacement preserves region eligibility and thread serialization while making paths, phases and failure classifications explicit.

## Goals / Non-Goals

**Goals:** implement the complete requested geometry and stage progression, bounded ownership/cleanup, six observable failure categories and faithful local geometry/timing tests; verify native tools, neighboring ink and history interactions.

**Non-Goals:** retrieval/spokes implementation, follow-up recognition, page insertion, a new history protocol, firmware/account changes or a pixel overlay that pretends to restore document ink.

## Decisions

1. **Three nested triangles.** Preparing follows clean trigger capture; AnswerPending begins at answer-request dispatch; AnswerReady follows its response and remains the final stage until rendering/failure. Each triangle has three ordered edges, nested about a common center in the existing 50x50 region with native-stroke clearance. Queue stage advancement until the prior triangle has its first traversal. HTTP dispatch proceeds while that traversal finishes on the caller thread; a fast response can precede the displayed transition. Finish queued traversals before auxiliary inference/navigation so completed nesting is still visible. Subsequent pending ticks retrace that stage to build darkness; opacity follows the verified temporary status style. Complete the ready traversal before clearing for navigation. On a clean valid successor, draw the complete current AnswerReady triangle at the same cadence before clearing for body-mode output; do not claim that a single fleeting edge is a completed stage. This adds at most a few bounded 333 ms intervals after fast responses; drawing every edge at once would violate the requested cadence.

2. **Auxiliary inference.** Independent transcription uses a closed incircle tangent to the innermost displayed triangle. The circle is refreshed during that request, while completed triangle paths remain. Use shared pure geometry; REM-4 can later add per-edge center spokes without introducing another device writer. Do not add unused retrieval behavior now.

3. **Cadence and serialization.** Schedule stroke starts against a monotonic device clock at 333 ms intervals, accounting for time spent drawing. Backend delay advances virtual time in simulation and sleeps natively. The HTTP worker performs only transport; caller-thread progress uses deadline-based waits rather than sleeping 333 ms after a stroke. Slow strokes skip missed deadlines without a catch-up burst. Tests cover initial traversal, repeated edges, transitions, delayed HTTP and overrun calculation.

4. **Finite owned-path ledger.** Record each unique triangle edge/auxiliary circle before the draw attempt. Cleanup erases only these paths, including potentially partial strokes, with bounded work independent of wait duration. Successful cleanup clears the ledger. Failure latches the existing fatal cleanup condition and prevents navigation/output/future iterations. All current clean-capture, navigation, body-mode, text and completion boundaries retain cleanup. Suppressed corners neither draw nor acquire erasure ownership. Tool preferences may be changed temporarily only through the verified style lease below; exact restoration precedes further document work. Native pen/eraser release is attempted on failure.

5. **Six persistent failure codes.** Constant X plus exactly one segment of a centered 25x25 box: top = missing/unreadable/ambiguous selection or malformed answer proposal; right = independent transcription invalid/disagrees; bottom = provider transport/timeout/unavailable; left = no successor movement; horizontal midpoint = unsuitable successor after confirmed recovery; vertical midpoint = device/render/recovery failure. Cleanup precedes the mark; eligible state is required. Unknown/occupied corners suppress marks. Cleanup failure suppresses further input entirely. Provider errors still propagate in single-iteration mode, and verification-provider errors remain conservative declines, but each now attempts its proper code. Loop mode logs propagated errors and continues without inserting arbitrary Error text. Failed recovery uses device code rather than claiming a confirmed invalid-page return. Prevent duplicate markers within one iteration.

6. **Simulator model.** Replace the single circle raster with the same bounded stroke paths, accumulate retracing darkness deterministically, expose stage/path/timing events and persistent error segments. Update fault operation names and tests intentionally. Keep temporary marks separate from document text/lines. Assertions for captures/navigation/output and history remain strict. Native brush width, persistence and e-ink refresh cannot be proven by this model.

## Risks / Trade-offs

- Small nested geometry under a broad highlighter can merge visually -> inspect native Fineliner and Highlighter output and adjust within the fixed region; use the verified black medium Fineliner lease and prove readable output and restoration.
- More native strokes can affect persisted scene history -> save immediate pre-append/applied/undo states, test empty and occupied corners, preserve conservative scene guards and record availability failures rather than forcing success.
- Erasure can affect nearby ink -> retain the 12-pixel eligibility clearance and bounded owned paths, compare original native content and screenshots.
- Fast-stage minimum traversal adds latency -> bounded by missing first-pass segments; never sleep through provider work unnecessarily or run device operations concurrently.
- Device failure may leave no safe place for an error code -> log and propagate as applicable; safety takes priority over guaranteed visibility.

## Migration Plan

No stored content migration. Existing old marks remain user document content and make their corners ineligible. Update code/specs/simulator together, build both targets, run authorized disposable-document native tests and restore the original document/header/service. Roll back by restoring the previously verified binary. Publish full native/test evidence and obtain exact-head review plus required CI before normal merge.

## Open Questions

Native legibility and the existing append-history availability interaction require empirical validation before closure. The six error categories are the concrete implementation mapping because the original request specifies geometry/count but not named conditions.


## Native finding during implementation

Runtime1ebb3b0 rendered all stages at about333 ms per uninterrupted stroke, but short eraser paths returned success while triangles remained after a settled recapture. The earlier circle-only native evidence did not prove short-edge erasure. Retain that failed run. Add explicit rubber proximity before contact, then validate cleanup by fresh native screenshot; residual/changed corner pixels latch failure before navigation or typing. Simulator StatusClear/no_move preserves this failure. Tool response and eraser fixes require new native proof before closure.

## Revision: scoped native status style

Native4105ff3 proved correct geometry/cleanup but Highlighter's roughly16-pixel footprint merged nested shapes and failure-code detail. This is not accepted as readable staged feedback. A narrow maintained-source assessment found smart_remarkable cb787065 src/touch.rs450-671 uses screenshot-driven tool selection. Its fixed older coordinates, assumed slot types and incomplete preference restoration are not copied. remarkable-mcp writes persisted ink, unsuitable for live333ms status; XOVI adds an injected Qt extension and is outside this bounded correction. No directly usable native style service was exposed in the tablet DBus inventory.

Add a page-scoped status style lease, acquired once before the first status stroke or persistent failure mark and restored after owned cleanup or failure drawing, before capture/navigation/typing. The333ms loop performs no toolbar actions. Use current-document/page observation plus a fresh screenshot before each dependent UI action. Compare the content viewport against the pre-menu frame (excluding toolbar/status) to reject changed pages within available observability. No style work occurs during native history ownership; workflow invalidates history before beginning status.

Initial support is the actually observed RM2 3.28 portrait toolbar/menu contract. Hidden toolbar, already-open menu, unsupported layout, missing/ambiguous metadata or active non-pen tool refuse BEFORE mutation and suppress status; Reader analysis continues. Do not infer generic layout support from an architecture build. Snapshot the actual active slot and document/page identity before opening menus. Capture the primary grid before selecting Fineliner, then its actual color/width before changing either dimension, as specified in the current-UI revision below. Persisted tool metadata is advisory only. Persist a bounded recovery record before the first possible mutation. It contains no credential or document text. Create it exclusively with create-new semantics, owner-private permissions, a validated version and bounded fields; never replace an existing record, including an incomplete crash write. A leftover record stops automatic status mutation and reports the original preferences for deliberate recovery; do not claim process-crash finally guarantees or automatically overwrite subsequent user choices.

The lease opens the primary tool menu with screenshot-confirmed controls, durably records the actually selected grid index, selects Fineliner, captures its actual menu color/width, and uses black medium Fineliner strokes, whose narrow footprint produced distinct geometry in native4105ff3; medium is a tested tool setting rather than a generic claim about brush widths. Avoid changing width/color when already correct. Restore potentially changed Fineliner dimensions from captured UI values, the original primary grid and original active slot, verifying fresh supported controls. Never modify another tool preference or use stale file values as restoration targets. Close only menus opened by the lease. A primary Fineliner already using black medium can avoid temporary preference changes. Do not edit native config/content files to change tools.

Track each intended mutation before sending input because a failing call may have acted. Partial acquisition invokes bounded rollback using fresh observed state, not an assumed menu state. Restoration is required after successful cleanup, provider errors, partial status input and persistent failure drawing. An unknown changed page/layout or failed restoration latches a fatal condition and prevents further document input; retain the recovery record. Successful verified UI restoration removes only the owned, valid journal; journal I/O failure retains evidence. A failed error marker is never recursively retried.

Tests must cover partial acquisition at each action, preference/slot restoration, occupied/unknown layouts, already-open menus, page changes and no toolbar operations during history or repeated ticks. Native evidence must include Highlighter-start success and error cases, nondefault Fineliner saved preferences, restored primary/secondary selection, six distinct failure marks, and measured acquisition/restore overhead for REM9. Keep failed broad-tool captures as evidence motivating this design; do not relabel them as readable status.


Review/native refinement: closed-toolbar recognition now requires an exposed blank page-side seam and absence of selected menu-grid rectangles, in addition to a supported selected pen tile. A partial menu border or different docked popover is not classified as closed merely because the known border is absent. Page ink at the seam conservatively suppresses status. Active-slot probes avoid the upper-right color dot: native ffb0a1e Highlighter was safely suppressed because Yellow overlapped the original probe, not because geometry was readable. Native fixtures preserve this finding. Transition observations retry recognized UI nonconvergence up to five times; identity/viewport/layout errors immediately stop the transition and enter fresh bounded rollback rather than being ignored. A no-change restoration fast path requires previously captured actual UI values, a fresh same-owner/viewport observation, positively closed original controls and matching pen-slot pixels; file equality is never its proof. Record removal remains after verification.


## Revision: current UI is authoritative for style (native stale-preference finding)

Native47b secondary Highlighter with primary Fineliner saved Red/Thick remained visually current while the same confirmed document's content file still reported primary Fineliner Black/Medium and secondary Ballpoint. The file timestamp had not advanced since12:02UTC. Equality of those persisted snapshots is not proof of current UI preference restoration. The previous metadata-authoritative agreement/restore decisions are superseded. Existing geometry and visible primary Highlighter return evidence remains valid, but current-preference restoration acceptance is withdrawn until this revision is verified.

Use a bounded staged transaction, not a generic UI framework. Before any menu action, capture supported closed UI, actual active slot, document/page/session and page image. Persisted tool preferences are a bounded advisory snapshot only; stale tool values neither override known UI nor cause its rejection. Retain independent owner/viewport guards. Positively identify the current primary menu grid after opening our menu and durably record it BEFORE selecting Fineliner. Selecting a slot/tool for inspection is already mutation. If inspection fails before any color/width mutation, rollback restores only the captured original primary grid and active slot and closes only our menu; it must not write color/width using stale metadata.

After selecting Fineliner, read its actual supported color and width from the settled menu. Durably capture those values BEFORE any color/width input. Mark each potentially changed dimension before sending its press, including partial failures. Restore only potentially changed dimensions from the captured UI values, then verify actual Fineliner menu values, original primary grid and original active slot in fresh screenshots. Unexpected UI/page changes stop input. The no-change fast path requires the corresponding captured UI values, same owner/viewport and positively closed original controls; advisory file equality is not the proof.

Recovery becomes a versioned, bounded, append-only sequence of durable snapshots in the exclusively created owner-private recovery file. Each complete line records immutable initial identity/active slot/advisory metadata, any subsequently captured primary grid/Fineliner values, the next input phase and monotonic may-have-mutated flags. A primary-grid checkpoint precedes tool selection; a Fine-style checkpoint precedes color/width changes. Every input intent is flushed before input. Limit records/bytes, parse and validate versions/sequence and captured-value consistency; a partial/crashed record is unresolved and blocks later automatic mutation. Hold the originally created file handle and verify ownership before append/removal; never truncate, replace or ignore an existing unresolved record. Successful verified restoration removes only this lease's record. A crash during probing is distinguishable from one after color/width changes, and deliberate recovery never applies stale metadata as current UI values.

Recognized post-input UI lag is observed a bounded number of times. Document/page/session/viewport loss is fatal, not retried past. An acquisition failure with positively verified rollback suppresses status and allows normal analysis; unsuccessful rollback/restoration stops all further tablet input and retains recovery evidence. Repeated333ms ticks perform no toolbar or journal operations. No forced save, page flip, config-file edit, injected Qt extension, or wider native API investigation is introduced.

Tests must separate actual UI preferences from intentionally stale persisted values (actual Red/Thick versus file Black/Medium), use both original active slots and different primary/secondary tools, and inject failures before/after every probe, setting and restoration input. Check that rollback never changes unmodified color/width, unknown Fine styles restore just the inspected tool/slot, durable checkpoints precede possible mutation, unrelated current preferences survive, and current UI evidence, rather than advisory file equality, establishes restoration. Native tests must demonstrate nondefault settings before/after both successful activity and a failure mark, including secondary Highlighter and already-open-menu refusal. Preserve all prior stale-file findings.


The journal reserves twelve records and their maximum byte capacity for rollback, flushes initial content and the Linux parent directory before first input, and latches any ownership/write/sync failure so evidence cannot be removed after such a failure. Cleanup/restoration failure also latches at the workflow entry to prevent the outer iteration cleanup from retrying tablet input after an earlier failed restoration.

Reader construction checks for an unresolved journal before trigger dismissal or any workflow UI input; a later acquisition check also protects against another writer creating a record after construction. The outer orchestrator preserves the original error when cleanup/restoration is poisoned instead of retrying input or hiding the original cause.

Any failed durable checkpoint, including capacity rejection, poisons the lease before further observation or input. The no-change restoration fast path cannot bypass this latch. Reserved capacity supports normal bounded rollback after input or observation failure; it does not authorize continuation after a rejected checkpoint. Retain the journal for deliberate recovery.

Diagnostic refinement after final2544 paper-page refusal: retain the exact lease baseline and latest rejected page frame when the existing explicit READER_BUDDY_DEBUG_DUMP option is enabled, using two fixed bounded PNG paths. This is opt-in local diagnostic evidence, never model input, and does not change acceptance thresholds, masks, ownership or rollback behavior. Preserve each run outside /tmp before a later deliberate run overwrites the diagnostic pair. Reproduce unchanged-page capture and erasure effects before proposing any tolerance; tiny genuine content changes must not be accepted merely by pixel count.

### Restore tools before intentional eraser redraw (native evidence revision)

Final2544 and diagnosticbad866b show unchanged paper pixels throughout status drawing and menu-only controls, but native rubber erasure redraws a small set of printed glyph pixels outside the status corner. Lower pressure263 still redraws those pixels and is not a proposed production fix. Nearby native sentinel strokes, including just outside the clearance boundary, survived the tested original ordering exactly. These observations do not justify accepting arbitrary small content changes or globally relaxing the page comparison.

Revise temporary cleanup to restore and verify the captured original style under the unchanged strict full-page guard BEFORE eraser input. Retain the lease/journal and durably record PendingCleanup (tools verified restored, erasure may be incomplete) before cleanup. Failed restoration emits no erase input and retains owned marks and recovery evidence. Immediately before erasing, capture fresh bracketed native session/owner and closed original controls, preserving an exact pre-erase image. Erasure uses the original physical rubber operation independently of the restored selected pen. Persistent failure marks retain their non-erasing restoration path.

After erasure, require the same freshly bracketed session/document/page/visit, positively closed original toolbar, original pen-slot pixels and an eligible clean corner. Verify viewport invariants by strict pixel comparison against the immediately pre-erase frame over x>=61 except the qualified lower-right192x224 region (x>=576,y>=800; narrowed by the observability refinement below). No former menu exclusion applies because menus must be closed. The isolated native PDF redraws were confined within this region; image comparisons outside it were exact. This guard verifies the invariant page region, not preservation of every pixel within the excluded redraw region; off-corner annotation preservation there remains an empirical native gate with sentinel strokes at multiple positions, and the before-restore full-page guard remains strict. Do not call this proof against all concurrent edits or all layouts. Real one-pixel viewport shifts and changes outside the redraw region, owner/session changes, menu/tool drift and residual marks must still fail.

Only successful post-cleanup verification removes the owned journal. A cleanup or finish failure retains it, halts all further input and is never retried by outer cleanup. Model restore-before-erase-before-finish explicitly, including failure at each boundary and startup refusal for PendingCleanup. Revalidate original secondary Highlighter and nondefault Fine restored before erasing, neighboring ink and actual PDF behavior on the new ordering before acceptance. This changes the post-erase verification contract; it does not claim the older ordering's native tests validate the revision.

Observability refinement before implementation: the post-erase redraw exception is NOT available for arbitrary pages. Before any status UI mutation, accept either a completely blank non-toolbar canvas (which must pass strict full-canvas post-cleanup comparison with NO redraw exception), or sufficient dark/light page landmarks in three separated noncollinear regions outside the smaller empirical redraw region x>=576,y>=800. Sparse nonblank pages and pages whose only content is within that region suppress status before mutation; normal Reader analysis continues. For landmark-qualified pages, first try the strict complete post-cleanup page comparison; only the empirically confined redraw differences may use the invariant-region check. Require unchanged pixels throughout that invariant region, including areas formerly covered by the now-closed menu. The bound is an empirical supported region, not a proven Qt tile boundary or eraser radius; a redraw outside it still fails. Preserve negative translation/zoom cases on sparse and confined-content pages, and explain that truly blank-to-blank translation is unobservable under both the previous and retained strict blank-image contract.

Fixed landmark classifier: three256x256 patches with top-lefts(96,128),(416,128),(96,576), each divided into sixteen64x64 cells. A cell requires at least16 pixels below200 and at least128 pixels at or above248. Each patch requires at least6 qualifying cells spanning at least3 distinct rows and3 distinct columns. These fixed contrast/coverage rules require two-dimensional structure in all three noncollinear regions; isolated specks, one uniform edge and toolbar/status-only landmarks cannot qualify. Completely blank means all page pixels x>=61,y>=61 meet the existing blank threshold248; it uses strict post-erase comparison only. The dense native fixture's measured cell counts are recorded in local evidence, not used to tune a per-page classifier.
